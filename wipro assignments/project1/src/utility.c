// tar_handler.c

#include <stdio.h>
#include <utility.h>

void listContents(const char* filename) {
    // Implementation for listing contents of a tar archive
    printf("Listing contents of tar archive: %s\n", filename);
    // Add code to list contents of the tar archive
}

void listContents(const char* filename) {
    // Implementation for listing contents of a zip archive
    printf("Listing contents of zip archive: %s\n", filename);
    // Add code to list contents of the zip archive
}
